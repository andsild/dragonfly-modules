language: 
  - python
python:
  - "2.7"
before_install:
  - lsb_release -a
  - sudo apt-get install cabal-install ghc stack
  - cabal update
install: 
  - ./installSubmodules.sh
  - pip install -r requirements.txt
# command to run tests
script: unit2
